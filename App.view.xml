<mvc:View
  controllerName="HelloWorld.App"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns="sap.m">
      <VBox>
      <Panel
            headerText="Please enter you thought."
            width="100%">
            <content>
              <TextArea
                growing="true"
                valueLiveUpdate="true"
                value="{InputModel>/InputValue}"/>
              <Button
                text="Submit"
                press="saveNote"/>
            </content>
      </Panel>
      <Text text="{InputModel>/InputValue}"/>
      <Button
        id="helloButton"
        type="Emphasized"
        icon="sap-icon://sap-ui5"
        text="Say Hello"
        press="onShowHello"
        class="sapUiSmallMargin"/>
      </VBox>
</mvc:View>